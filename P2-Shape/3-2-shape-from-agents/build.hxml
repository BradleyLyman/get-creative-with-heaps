-cp src
-cp ../../tools
--main Main
--library heaps
--each

# Build the demo js
--next
--js bin/demo.js

# Build the HashLink Executable
--next
--library hlsdl
-D windowSize=1366x768
--hl bin/app.hl

# Run the HashLink Executable
--cmd "hl ./bin/app.hl"