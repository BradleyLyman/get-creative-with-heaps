-cp src
-cp ../../support/src/
--main Main
--library heaps
--each

# Build the HashLink Executable
--next
--library hlsdl
-D windowSize=1920x1080
--hl bin/app.hl

# Run the HashLink Executable
--cmd "hl ./bin/app.hl"